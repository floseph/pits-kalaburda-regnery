const express = require('express')
const app = express()
const port = 3000

// MIDDLEWARE

// ROUTES

// USING MIDDLEWARE

// USING ROUTES

app.listen(port, () => {
  console.log(`Listening on Port ${port}`)
})